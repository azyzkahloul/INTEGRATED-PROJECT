<form method="post" action="{{ path('amande_delete', {'id': amande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amande.id) }}">
    <button class="btn btn-info">Supprimer</button>
</form>
