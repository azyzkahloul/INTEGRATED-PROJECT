<form method="post" action="{{ path('facture_delete', {'id_facture': facture.id}) }}" onsubmit="return confirm('Voulez vous supprimez la facture?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.id) }}">
  <button class="btn btn-info">Supprimer</button>
</form>
