{% extends'baseBack.html.twig' %}
{% block body %}
    <div class="content-wrapper">
        <div class="container-fluid">

            <div class="row mt-3">
                <div class="col-lg-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Liste des agents de service</h5>
                            <div class="table-responsive">
                                <table class="table table-striped">

                                {{ include('agent_service/_form.html.twig') }}
                                <a href="{{ path('app_agentservice') }}">Liste des agents de service</a>
    </div>
    </div>
    </div>
    </div>
    </div>
    </div>
    </div>

{% endblock %}